const auth = require('./auth');
const catchError = require('./catchError');
const handleErrors = require('./handleErrors');
const logErrors = require('./logErrors');
const logParametrs = require('./logParametrs');

module.exports = {
  auth,
  catchError,
  handleErrors,
  logErrors,
  logParametrs
};
